{#-

  Jinja2 Template for a visitor interface
-#}
package {{ doc.package }};

/**
 * Visitor interface for {{ gen.get_class_name(doc.visited_object) }} objects.
 */
public interface {{ doc.name }}<T> {
    public T visit({{ gen.get_class_name(doc.visited_object) }} visited);
{%- for child in gen.get_descendants(doc.visited_object) %}
    public T visit({{ gen.get_class_name(child) }} visited);
{%- endfor %}
}
